# 蓝绿部署 - 服务切换
# 通过修改 selector 的 version 标签来切换流量
apiVersion: v1
kind: Service
metadata:
  name: backend-bluegreen
  namespace: ecommerce
spec:
  selector:
    app: backend
    version: blue  # 切换到 green 时修改此值
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
